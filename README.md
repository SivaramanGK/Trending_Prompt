# Trending_Prompt
index.html fetches data/prompts.xlsx directly in the browser (no build step, no server) using a small library called SheetJS. Whatever rows have Yes in the Trending (Yes/No) column show up as cards on the site, in the order set by the Order column.
